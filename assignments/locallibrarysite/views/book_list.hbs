<h1>{{title}}</h1>

{{#if book_list}}
    <ul>
        {{#each book_list}}
        <li><a href="{{url}}">{{title}}</a> - {{author.name}}</li>
        {{/each}}
    </ul>
{{else}}
    <p>There are no books</p>
{{/if}}